{
  "name": "active-report-js-test",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "front",
    "server"
  ],
  "scripts": {
    "dev": "yarn run run-p dev:server:*",
    "dev:server:build": "yarn run gulp server",
    "dev:server:start": "node server/server.js"
  },
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-typescript": "^6.0.0-alpha.1",
    "jsonc-parser": "^3.0.0",
    "node": "^17.1.0",
    "ts-node": "^10.4.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.11",
    "yarn-run-all": "^3.1.1"
  }
}
